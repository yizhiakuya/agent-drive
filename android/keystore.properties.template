# 复制本文件为 keystore.properties（已 gitignore），按本机实际路径填写。
# keystore 从服务器取：/root/agent-drive-android/agentdrive.keystore（密码在服务器本地 README.txt）
# 注意 Properties 格式：反斜杠要双写，冒号前要加反斜杠。
storeFile=D\:\\ds\\agent-drive-keystore\\agentdrive.keystore
storePassword=CHANGE_ME
keyAlias=agentdrive
keyPassword=CHANGE_ME
