<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgentDrive"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 分享到网盘：接收系统分享并 POST 到前端 manifest 的 share_target -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- TWA 配置：打开地址 / 状态栏色 / Digital Asset Links（免地址栏验证） -->
            <meta-data
                android:name="androidx.browser.trusted.DEFAULT_URL"
                android:value="https://home.rainaki.top:13311/" />
            <meta-data
                android:name="androidx.browser.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="asset_statements"
                android:resource="@string/asset_statements" />
        </activity>
    </application>
</manifest>
