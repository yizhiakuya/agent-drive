<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdrive.infrastructure.persistence.mapper.CredentialMapper">
    <select id="selectSessionOwner" resultType="map">
        SELECT user_id::text AS user_id
        FROM sessions
        WHERE credential_hash = #{credentialHash, jdbcType=VARCHAR}
          AND revoked_at IS NULL
          AND expires_at &gt; now()
        LIMIT 1
    </select>

    <select id="selectDeviceOwner" resultType="map">
        SELECT user_id::text AS user_id
        FROM devices
        WHERE device_token_hash = #{credentialHash, jdbcType=VARCHAR}
          AND revoked_at IS NULL
        LIMIT 1
    </select>
</mapper>
